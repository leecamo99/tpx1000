# 資料編輯模式 v1.2 — 無損高亮

## v1.2 修正

- GitHub 寫回不再重新序列化整個 `data` 陣列；現在只替換指定 `id` 的單一物件，其餘 `books/kin1000.js` 位元內容保持不變。
- 儲存前驗證高亮依賴的 `word`、`mean`、`en`、`ja` 與各 sense 的 `pos`、`mean`、`en`、`zh` 型別和結構。
- 保留條目與 sense 的全部未編輯欄位，避免高亮、音檔或其他功能依賴的 metadata 遺失。
- 以第 078 筆 `install` 驗證：修改並儲存後重新啟動瀏覽器載入資料，英文 `install` 與中文 `安裝` 仍維持原橘色高亮。

# 資料編輯模式 v1.1 — 句子欄位

## 根因修正

- 最新單檔版原本使用內嵌的 `KIN1000_DATA`，`books/kin1000.js` 並未被載入。
- 現改為優先載入 `books/kin1000.js?v=<timestamp>`；只有載入失敗才使用內嵌備援。

## v1.1 新增

- 主要英文例句與繁體中文翻譯改為清楚分組的可編輯欄位。
- 多義條目會為每個 sense 產生獨立的英文例句與繁中翻譯欄位。
- 儲存採欄位合併，不會用編輯表單重建整筆資料；未編輯的條目 metadata 和 sense metadata 均保留。
- 若遠端 sense 數量在編輯期間發生變化，會停止寫入，避免把句子套到錯誤義項。

## 使用方式

1. 點右下角「工具」。
2. 在「資料編輯」按「啟用」。
3. 開啟要修正的單字，按「編輯這筆資料」。
4. 修改後按「儲存並覆寫 GitHub」。

## 儲存策略

- 一般書籍：先讀 GitHub 最新檔案和 SHA，只替換相同 `id` 的資料，再提交回書籍 JS 檔。
- 我的收藏：更新瀏覽器內的閃卡資料，並立即呼叫原本的 GitHub 學習紀錄同步。
- GitHub 更新成功後，畫面立即重繪。
- 書籍 JS 改採 network-first 且載入時 cache-bust，避免手動更新後仍看到舊資料。

## 安全限制

- 編號與級別維持唯讀，避免破壞音檔、學習紀錄與篩選對應。
- 寫入前會再次確認目標路徑和編號。
- 使用 GitHub SHA 做 optimistic locking；發生 409 時不會覆蓋他人的較新版本。
- Token 只沿用既有的瀏覽器 localStorage 設定，不會寫入專案檔。
