# TPX1000 v3　詞義級 SRS　更新包

## 這是增量更新包，不是完整專案

只含有變更的檔案。解壓後覆蓋到你現有的專案資料夾即可，
books/、books.js、audio/、data/ 都不需要動。

    index.html              覆蓋
    sw.js                   覆蓋（版本已改為 kin-shell-v3）
    manifest.webmanifest    覆蓋
    icons/                  覆蓋（4 個檔）

覆蓋後照常 git add . && git commit && git push。

## 新增：詞義級 SRS

多義字（62 個字、共 169 個詞義）現在可以逐個意思標記熟／不熟。

解說頁每個例句下方出現「這個意思　熟悉 ✓ ／ 不熟 ❗」，
右側顯示該意思的下次複習日。再點一次同一顆可取消標記。

彙總規則：
    任一意思標「不熟」  整字維持「要複習」
    全部意思標「熟悉」  整字轉為「習得」
    只標了一部分        不動整字狀態，保留你原本的判斷

按整字的「習得／要複習／未學習」時會同步覆寫全部意思，兩套操作不會打架。

每個意思各自跑 SM-2，有獨立的 ease / interval / reps / lapses / due，
所以「接受、配合」可以每天考，「容納」排到兩週後，互不干擾。

其他連動：
- 一覽頁：多義字顯示「義 1/2 不熟」標籤
- 測驗：新增出題範圍「弱點詞義」；原本的「弱點字」也已涵蓋有弱詞義的字
- 儀表板：新增「不熟的詞義」清單，依遺忘次數排序，可點擊跳到該字
- 深色模式已一併處理

## 同時修正

匯出進度檔原本只帶 status / cards / log，v2 之後新增的收藏、筆記、
測驗紀錄、成就、學習時間、目標達成日都不在裡面，用檔案換手機的人
會整批遺失且沒有提示。現已全部補上，並加入詞義級排程。合併規則：

    收藏      依單字聯集
    筆記      取 at 較新者
    測驗紀錄  去重排序，上限 300 筆
    成就      取較早的取得日
    學習時間  同日取較大值（避免重複匯入灌水）
    詞義排程  取 last 較新者

舊版進度檔仍可正常匯入。

## 資料位置

詞義級資料存於 localStorage 的 kinSense:<使用者>，
已納入 GitHub 同步清單與匯出／匯入。

## 提醒

日後每次改 index.html，記得把 sw.js 第 6 行的 VER 往上加，
否則舊的 Service Worker 會繼續餵快取裡的舊版。
