TPX1000 GitHub 題庫編輯器 v1.3／v62 驗證
=======================================
驗證日期：2026-09-18

本版不是只保護序列化，而是追查並修正實際高亮流程：
showDetail → exWordsHtml / highlightZhMeaning → __kinPaintNoteWords → 編輯儲存後重新建立 currentList → showDetail → 延遲 UI wrapper。

PASS  43 段 inline JavaScript、books.js、shiko600.js、sw.js、books/kin1000.js 全部通過 node --check。
PASS  英文高亮改用詞形集合；install 可匹配 install、installs、installed、installing。
PASS  英文目標字節點帶 data-target-word="1"；中文對應詞義帶 data-target-meaning="1"。
PASS  每次 showDetail 都會從目前 entry 重新計算高亮。
PASS  GitHub PUT 成功後會從 DATA 重新定位該 id、重畫解說，並在同步、0ms 與 180ms 三個時點重算高亮，以涵蓋後續 wrapper。
PASS  熟悉度／收藏重新套用 class 後，語意高亮仍以 !important 保持橘色。
PASS  GitHub 寫回只替換指定 id 的單一物件，不重新序列化整個 data 陣列。

install 第 078 筆必過案例（1180 × 820 寬版）：
- 修改英文例句為：The technician installed the air-conditioning equipment.
- 修改繁中翻譯為：安裝空調設備
- 儲存後等待 1.2 秒（涵蓋延遲 wrapper）：installed 與「安裝」均為 rgb(196, 92, 26)。
- 把 GitHub PUT 產生的內容寫成新的 books/kin1000.js，關閉瀏覽器並以全新瀏覽器載入：installed 與「安裝」仍為 rgb(196, 92, 26)。
- 英文 HTML 保留 class="nw hl" 並有 data-target-word="1"。
- 中文 HTML 保留 class="zh-hl" 並有 data-target-meaning="1"。
- 全程無 console error / page error。

PASS  多詞義條目 S6-2 的主例句與 3 組 senses[n].en / senses[n].zh 仍可正確寫回，metadata 與相鄰條目均保留。
PASS  驗證截圖：install-078-highlight-runtime-v62.png（1180 × 820）。

TPX1000 v63 additional validation
----------------------------------
PASS  index.html: all 43 inline scripts pass node --check.
PASS  books/kin1000.js: node --check passes; 1,817 records load.
PASS  Entries 077 and 078 contain no escaped or decoded NUL characters.
PASS  Latest edits retained for entries 077, 078, 079, and 146.
PASS  Browser file:// test: 1,817 list items rendered; entry 078 detail rendered.
PASS  Runtime highlight test: "install" received data-target-word and remained highlighted.
PASS  Editor rewrite test: recursive NUL removal and CRLF normalization succeeded.
PASS  Browser console errors: 0; failed requests: 0.
PASS  Screenshot: verification-v63.png (1180 × 820).
